# OhlFinalProjectWinter2025

An implementation of a simple programming language using Rust.

Contains a lexer, parser, analyzer, and evaluator. 

Takes in input in a specific grammar from a .txt file and evaluates it.
